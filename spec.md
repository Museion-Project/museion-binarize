# Spec：M PDF 处理器产品与技术规格

**状态：** Active，产品意图已确认，按 [`plan.md`](plan.md) 分阶段实施

**日期：** 2026-08-26

**当前实现基线：** `0.1.0-rc.2`

**实施计划：** [`plan.md`](plan.md)

本文把产品意图转换为可审查、可实施的目标行为和架构。文中的 **必须**、**应该**、
**可以** 分别表示发布必需、强烈建议和可选。标为“当前”的内容已经存在；标为“目标”
的内容尚未因此文件而自动成为已实现能力。

## 1. 范围与发布边界

### 1.1 当前基线

当前仓库已经包含：

- Rust `mpdf-core`：PDFium 栅格化、预处理、Otsu/Sauvola/manual 二值化、
  despeckle、1-bit 图像、CCITT Group 4 和 PDF 重建；
- `mpdf-cli` / `mpdf`：`info`、`inspect`、`analyze`、`estimate`、`preview`、`process`、
  `benchmark`；
- Tauri 2 + React 桌面端：单文档打开、缩略图、前后预览、估算、处理、取消和结果报告；
- 版本化 JSON 报告、合成基准、PDFium 分发和跨平台打包基础；
- 本地处理，无 OCR、AI、书签、扫描矫正、遥测或业务网络请求。

### 1.2 目标发布序列

- **Release A — M PDF Processor 基础版：** 公开、验证和稳定当前能力，不借重命名暗示
  OCR 或书签已经存在；
- **Release B — M PDF 处理器 OCR 桌面原型：** MDP 0.x、坐标化 OCR、古希腊文评测、
  可核验书签和可搜索 PDF；
- **Release C — 手机最小产品：** 从夸克/文件/照片/分享入口导入，在手机端完成本地处理
  和专业 PDF 导出；
- **Release D — 内置扫描：** 系统文档相机起步，逐步增加面向书籍的几何和光照处理；
- **Institution：** 单机格式和质量稳定后，增加托管队列、多引擎、协作、治理和 API。

每个 Release 必须只宣传已经通过本文件规定验证门的能力。

## 2. 产品行为

### 2.1 通用工作流

目标工作流必须允许用户从 PDF 或页面图片开始，选择一个或多个处理结果：

```text
PDF / 页面图片 / 手机扫描
          │
          ▼
不可变输入快照 + source identity
          │
          ▼
几何校正（可选）→ 灰度/彩色母版
          │
     ┌────┼──────────────┐
     ▼    ▼              ▼
OCR候选  1-bit派生物    缩略图/预览
     │    │
     └────┼──→ 文字、坐标、版面、页码
          │
          ▼
证据化目录与书签
          │
     ┌────┼──────────────┐
     ▼    ▼              ▼
可搜索PDF MDP            结构化导出
```

- 用户必须能只运行二值化，保持现有快速路径；
- OCR、书签和 MDP 必须可以分别启用，不强制所有用户承担模型和存储成本；
- 默认设置必须保守，不为视觉“更白、更锐”牺牲古希腊文附加符号或校勘细节；
- 原始输入不得被原地覆盖；输出覆盖默认关闭并继续使用安全临时文件策略；
- 任何失败必须区分输入错误、引擎不可用、单页失败、资源不足、用户取消和输出验证失败。

### 2.2 Binarize

- 当前 Otsu、Sauvola、manual、预处理和清理行为必须保持可调用和可复现；
- 真正 1-bit `/CCITTFaxDecode` 输出必须继续作为一个明确输出模式；
- 二值化是 OCR 候选之一，不得成为 OCR 的唯一输入；
- 现有 `process` 的无 OCR 输出在相同版本、输入和设置下应继续保持字节确定性；
- 加入文字层、书签或 provenance 后的“专业 PDF”不承诺与纯二值版字节相同，但其内容
  顺序和 schema 必须确定；运行时长和外部模型结果除外。

### 2.3 OCR

#### 输入候选

每页 OCR 至少可以获得下列候选中的一个或多个：

- 原始或经方向归一化的页面；
- 保守灰度/背景校正母版；
- 保守全局二值版；
- 自适应二值版。

引擎选择器必须记录每个候选的处理参数，不得只保存最终文本而丢失输入来源。第一版可以
按引擎置信度和规则选择单一结果；多引擎融合必须晚于可复现评测，不能仅凭视觉模型主观
裁决。

#### 输出

OCR 页结果必须包含：

- 文档页索引和稳定页 ID；
- 识别语言；古希腊文使用独立语言标识，不与现代希腊文混同；
- 文本块、行、词，及引擎能可靠提供时的字形级坐标；
- 坐标系、页面尺寸和从 OCR 输入到母版页面的变换；
- 原始引擎文本，以及可选的 Unicode 规范化文本；不得不可逆地只保留规范化结果；
- 引擎名、引擎版本、模型/语言包版本、输入候选、参数和置信信息；
- 机器结果、AI 修订和人工修订的分层记录。

OCR 引擎必须通过可替换接口接入。本地候选包括 Kraken 与 Tesseract `grc`；Gemini Flash
等视觉模型只作为可配置的疑难页分析、结构推断或校订提供者，不能写死为产品能力。

#### 古希腊文保真

评测必须分别报告：

- 基础希腊字母错误；
- 重音、气息、分音符、下标 iota 等附加符号错误；
- 标点、上标、脚注标记和校勘符号错误；
- 多栏、脚注和 apparatus 区域的识别与阅读顺序。

任何“支持古希腊文”的发布说明必须链接到具体模型版本、数据集版本和评测报告。

### 2.4 Bookmark

目录/书签生成必须优先使用可检查证据：

1. 原 PDF 已有但可安全读取的书签；
2. 目录页中的标题、印刷页码和 OCR 坐标；
3. 正文标题候选、字体/版面层级和页面位置；
4. 用户确认或修订。

每个候选书签必须包含：

- 标题、层级和目标文档页；
- 可选印刷页码；
- 证据类型、证据页和文本区域；
- 置信状态：`confirmed`、`suggested` 或 `needs_review`；
- 生成者和修订来源。

系统不得因为目录页缺失或页码映射不确定而静默虚构章节。低置信条目必须进入审阅界面；
用户必须能编辑标题、层级和目标页，并把修订写回 MDP。

### 2.5 可搜索 PDF

- 页面视觉层可以是当前 1-bit CCITT 页面，也可以是保守灰度/彩色母版；
- OCR 文字层必须与页面坐标对齐，并在常见 PDF 阅读器中可选择、复制和搜索；
- 文字层不得改变页面可见外观；
- 书签必须写入标准 PDF outline；
- 输出必须重新打开并验证页数、几何、可渲染性、文字层存在性和书签目标范围；
- 产品必须明确说明它重建 PDF，而不是保留源文件全部交互对象；
- 默认不得把 OCR 文本写入文档元数据或文件名，避免意外泄露内容。

### 2.6 Machine-readable Document Package（MDP）

MDP 是开放、版本化的逻辑文档模型。0.x 阶段可以使用目录或 ZIP 容器，但逻辑模型必须
先稳定，不能让容器选择决定数据模型。

最低实体包括：

- `manifest`：schema、版本、文档 ID、创建工具和完整性摘要；
- `source`：输入类型、内容摘要、页面计数和可选原件引用/内嵌副本；
- `pages`：稳定页 ID、页序、印刷页码、几何、旋转和派生物关系；
- `assets`：原图、母版、二值图、缩略图的 MIME、尺寸、摘要和生成步骤；
- `ocr`：文字层级、坐标、语言、引擎、版本、置信与修订；
- `layout`：栏、段落、标题、脚注、apparatus、图表等区域及阅读顺序；
- `outline`：书签树、目标和证据；
- `provenance`：每个处理步骤的输入、输出、参数、软件/模型版本和执行位置；
- `validation`：schema、引用、摘要和质量检查结果。

#### 坐标规则

- 每个坐标对象必须声明所依赖的页面资产和坐标空间；
- 标准页面坐标应归一化到方向已校正、左上角为原点的母版空间；
- 像素坐标必须同时携带母版宽高；PDF points 的映射必须显式记录；
- 裁剪、旋转、透视或展平不得通过覆盖坐标静默丢失，必须保存变换关系；
- 页 ID 在重新运行某个派生步骤时应保持稳定，除非源页面集合改变。

#### 开放与互操作

- MDP schema、验证器和迁移工具必须开源；
- 优先映射 ALTO、PAGE XML、hOCR、PDF 和 JSON 等现有标准；
- 导入导出不要求使用产品云账号；
- 未知的向后兼容字段必须被保留或安全忽略，不得导致整个包不可用；
- 包内路径必须是相对路径，不允许越出包根目录。

### 2.7 桌面端

- CLI 与 GUI 必须调用同一处理核心；同一引擎和设置的结果不得因前端不同而分叉；
- 本地模式无需登录，不调用业务网络，不要求订阅；
- CLI 允许脚本循环和本地批量使用；不人为限制文件数；
- GUI 应提供“快速二值化”和“专业 PDF”两个清晰任务，而不是一次展示所有高级参数；
- OCR 模型不可用时，二值化功能仍必须工作，并给出安装/选择模型的可操作说明；
- 用户可以查看每页 OCR 与书签置信、跳到证据页并人工修订。

### 2.8 手机端

#### Release C：导入优先

- 接收系统分享、文件 App、照片库以及夸克导出的 PDF/图片；
- “Open in M PDF Processor” 必须能够从导入直接进入处理任务；
- 默认保留导入原件或其不可变快照，直到用户明确删除；
- 本地完成可支持的二值化、OCR、书签和 PDF/MDP 导出；
- 设备资源不足时必须允许降并发、暂停、恢复或转移到用户明确选择的云任务。

#### Release D：内置扫描

- iOS 首先使用 VisionKit 文档相机，Android 首先使用 ML Kit 文档扫描器；
- 系统扫描结果必须进入与导入文件相同的 MDP 和处理管线；
- 后续自研优先级依次为双页分割、透视/倾斜、光照/阴影、书页展平；
- 学术保真模式不得使用未标记的生成式超分辨率补造字形或附加符号。

#### 可选夸克云增强

- 必须由用户按任务明确启用，并说明页面会上传给第三方；
- API secret 只能存在服务端密钥管理中；
- 调用前必须通过合同、地区、数据保留、输出权利、价格、分辨率和质量评测门；
- 夸克输出是一个有 provenance 的派生资产，不替换原件；
- 夸克 OCR 不作为古希腊文默认引擎，除非未来专项评测证明并明确列出支持范围；
- 按调用计费能力必须有额度、成本上限和失败回退，不得包含在无限买断承诺中。

### 2.9 机构端

机构版必须建立在相同 MDP 和处理核心上，增加：

- 可恢复任务队列、多机并发、优先级和配额；
- 本地、私有部署或经批准云环境中的多 OCR/AI 提供者调度；
- 多用户、SSO、角色权限、审计和共享校订；
- 存储、保留、删除、备份、迁移和成本策略；
- API、webhook、可观测性、SLA 和支持。

机构订阅结束后，用户仍必须能导出标准 PDF、MDP 和结构化文本。服务不得创造与开源
工具不兼容的专有文档格式。

## 3. 目标架构

### 3.1 组件边界

```text
┌─────────────────────────────────────────────────────────────┐
│ 前端：CLI | Tauri Desktop | iOS/Android | Institution API  │
└──────────────────────────────┬──────────────────────────────┘
                               │ project-owned DTO / job spec
┌──────────────────────────────▼──────────────────────────────┐
│ 文档编排：source → assets → OCR → layout → outline → export│
└──────────────┬──────────────────────────────┬───────────────┘
               │                              │
┌──────────────▼──────────────┐  ┌────────────▼───────────────┐
│ 确定性本地核心              │  │ 可替换 Provider            │
│ raster/image/bilevel/PDF    │  │ OCR / AI / cloud enhance   │
└──────────────┬──────────────┘  └────────────┬───────────────┘
               │                              │ provenance
┌──────────────▼──────────────────────────────▼───────────────┐
│ MDP model + schema + validator + searchable PDF exporter   │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 现有核心的演进

- `mpdf-core` 继续拥有现有 PDFium 和二值化路径，避免一次性重写；
- 新增的 MDP、OCR、outline 和 searchable-PDF 类型必须是项目自有类型，不向前端
  暴露 PDFium、Kraken、Tesseract、Gemini 或夸克的 SDK 类型；
- Provider 接口不得让商业模型依赖进入纯二值化快速路径；
- 需要被手机复用的图像算法最终应从 PDFium 依赖中拆为更小的纯 Rust crate；现有 crate
  通过重导出或适配层保持源代码兼容；
- 移动端优先用原生系统组件完成捕获和 PDF/图片读取，再把方向归一后的页面资产交给共享
  核心，避免把桌面 PDFium 假设强加给手机；
- 机构编排、账号、计费和供应商密钥属于独立服务，不进入开源本地核心。

### 3.3 Provider 接口

每个 OCR、AI outline 或云增强 provider 必须：

- 声明能力、语言、版本、是否本地、是否确定、输入限制和成本类别；
- 接受由核心验证过的页面资产与资源上限；
- 返回项目自有 DTO，不把供应商响应直接持久化为唯一事实；
- 支持取消、超时和逐页错误；
- 生成 provenance，并允许保留原始响应用于调试时进行显式、受控选择；
- 不读取包内未授权文件，不根据 OCR 文本执行命令、打开链接或改变系统配置。

第一版接口应先用 fake/reference provider 完成契约测试，再接真实引擎。

### 3.4 数据与任务状态

统一任务至少包含：

`created → inspecting → preparing → recognizing → structuring → exporting → validating → completed`

并可从任何运行阶段进入 `cancelled` 或带结构化原因的 `failed`。不启用 OCR/outline 的任务
可以跳过对应阶段。持久化任务必须记录已经完成的页和派生资产，使手机和机构任务可以
安全恢复；当前桌面单次进程可以继续使用内存任务状态，直到恢复能力成为明确范围。

## 4. 兼容性

### 4.1 名称和应用标识

- 当前完整产品展示名为 **M PDF Processor / M PDF 处理器**；M 只是临时占位符，最终
  商店标题在发布前做关键词和商标检查；
- `binarize` 保留为功能、模块和兼容命令名；
- 已发布前可以调整包名，但一旦公开发布，现有 CLI 二进制名、Tauri identifier、签名和
  用户设置路径不得无迁移方案地改变；
- 当前尚无需要承诺兼容的稳定公开版本，M0 一次性迁移到 `mpdf` 技术占位名，不保留旧产品
  别名；正式发布后才启用稳定弃用策略；
- M0 使用中性的临时应用 identifier；最终 identifier 必须在首次商店提交前决定，并提供
  设置与文档迁移方案。

### 4.2 CLI 和 JSON

- 现有命令和参数在 Release B 中保持可用；
- 新能力优先通过新增命令或向后兼容的可选参数加入；
- M0 后的 `mpdf-*` schema 名是技术标识，不包含展示品牌；1.0 后不随正式命名批量重写；
- 新 MDP/OCR/outline 报告使用新 schema 名和独立版本；
- 增加字段可以保持同一 schema major；删除、改义或改变单位必须升级 schema major；
- 人类可读输出可以使用新品牌名，但脚本输出必须以稳定 schema 和字段为准。

### 4.3 PDF 与 MDP

- 当前纯二值 PDF 行为继续作为兼容模式；
- 专业 PDF 是新的输出 profile，不声称与旧 `process` 字节兼容；
- MDP 0.x 可以快速迭代，但每次破坏性变化必须提供迁移器或明确拒绝并给出版本错误；
- MDP 1.0 后遵循 major/minor 兼容策略；
- 所有坐标、页码和文字编码必须有跨平台 golden fixtures。

### 4.4 平台

- 当前真正运行验证的平台仍是 Apple Silicon macOS；Windows/Linux 在实际验收前不得仅因
  CI 构建成功而声称完整支持；
- OCR provider 和模型包必须按平台报告可用性，不得让一个平台缺失模型时整个应用崩溃；
- 手机端不承诺与桌面端相同吞吐量，但相同共享算法、输入和参数必须通过跨平台 fixture
  证明语义一致。

## 5. 安全与隐私

### 5.1 威胁模型

系统处理不可信 PDF、图片、压缩包、OCR 文本、模型文件和供应商响应。主要风险包括：

- 恶意 PDF/图片触发解析器或 PDFium 漏洞；
- 超大页面、解压炸弹、畸形坐标和深层目录造成资源耗尽；
- MDP 路径穿越、符号链接逃逸或摘要替换；
- OCR 文本中的 prompt injection 诱导 AI 或代理执行非文档操作；
- 客户端密钥泄露、日志泄露、缓存残留和第三方数据保留；
- 模型、语言包和 PDFium 等供应链资产被替换。

### 5.2 本地模式

- 默认不联网、不登录、不遥测；
- 前端不接收完整源 PDF 字节，继续通过受限 DTO 与后端通信；
- 所有尺寸、页数、像素数、坐标数、outline 深度和文本长度必须有上限；
- 输出继续使用同目录临时文件、刷新/同步、验证后持久化；
- 密码不得出现在 CLI 参数、进程列表、日志或报告；
- 模型和运行时必须有版本、来源和摘要；打包下载必须遵循现有 PDFium 的固定来源和校验原则。

### 5.3 MDP

- 解包前验证路径、条目数、声明大小和总解压大小；
- 拒绝绝对路径、`..`、设备文件和越界符号链接；
- manifest 引用必须留在包根内并与内容摘要匹配；
- provenance 中不得默认保存本机绝对路径、用户名、密钥或完整云响应；
- 内嵌原件必须是用户明确选择；外部引用不得在打开包时自动联网获取。

### 5.4 AI 与云服务

- 文档内容一律视为数据，不是系统指令；
- AI provider 只能返回约束 schema，不得获得 shell、文件系统写入或任意网络工具；
- 服务端凭据使用密钥管理，按 provider、环境和最小权限隔离；
- 上传前显示服务商、地区、用途和保留策略，取得任务级同意；
- 设置超时、重试上限、并发上限、费用上限和熔断；
- 云结果必须标记 provider、模型和时间，并可以删除；
- 不把用户文档用于模型训练，除非另有独立、明确、可撤回的同意和合同依据。

### 5.5 机构

- 必须提供租户隔离、角色权限、审计和可配置保留/删除；
- 任何“私有部署”或“不落盘”声明必须对应可验证部署配置和合同，而不是营销文案；
- 高校或图书馆数据跨地区传输前必须完成适用的数据保护与采购评审。

## 6. 开源与商业边界

- 当前 Rust 核心、CLI、桌面端、MDP schema、验证器和基准继续使用开源仓库；
- 当前 `MIT OR Apache-2.0` 许可暂不改变；它允许商业封装，也意味着不能依靠许可阻止
  他人重新打包，产品优势必须来自质量、品牌、工作流和服务；
- 付费手机应用可以使用开源核心，并对原生 UX、商店分发、维护和可选服务收费；
- 机构端的账号、队列、治理、托管和支持可以闭源，但必须读写开放 MDP；
- 若未来改变许可、接受需要特殊条款的贡献或引入模型权重，必须单独法律审查，不能在普通
  功能 PR 中顺带完成。

## 7. 开放问题与决策门

| 问题 | 必须决定的时间 | 当前倾向 |
|---|---|---|
| 最终展示名、商店副标题和仓库名 | M7 发布硬化前 | 当前只使用 M PDF Processor；仓库暂候选 `m-pdf-processor` |
| 最终应用 identifier | 首次商店提交前 | M0 使用中性 `mpdf` 占位 identifier，正式命名时迁移 |
| MDP 是目录、ZIP 还是两者 | MDP 0.1 ADR | 逻辑模型先行；目录用于开发，ZIP 用于交换 |
| MDP 是否默认内嵌源 PDF | MDP 0.1 ADR | 默认引用/摘要；用户选择内嵌，避免体积与版权问题 |
| OCR provider 的进程/FFI/服务边界 | 第一个真实 OCR 适配器前 | 先定义项目自有接口和 sidecar 契约 |
| 默认本地 OCR 引擎 | M3 首个闭环 | RapidOCR/ONNX 作为通用首接；古希腊文专项 provider 由 200 页基准决定 |
| Gemini 的默认用途 | Release B 评测后 | 疑难页、目录与校订，不作为唯一 OCR 真值 |
| 手机技术栈与 Rust 共享方式 | Release C 开工前 | 原生捕获 + 共享纯 Rust 图像/MDP 核心 |
| 手机 App 买断、订阅或混合 | 封闭测试前 | 本地功能买断；持续云成本单独计费 |
| 是否接夸克 API | 合同与样本评测后 | 可选抢救模式，不作默认依赖 |
| 机构端部署形态 | 首个设计伙伴确认后 | 托管与私有部署共享协议，避免提前造平台 |
| 真实评测材料许可与公开范围 | 建立 200 页基准前 | 公共领域/明确授权；私有集只公布方法和汇总 |
| 当前 crate 是否拆出纯图像核心 | 手机共享核心前 | 需要，但不阻塞桌面 Release B 原型 |

未解决问题必须留在此表或对应 ADR 中。实现不得用临时默认值把需要产品、法律或学术判断
的问题悄悄变成既成事实。
